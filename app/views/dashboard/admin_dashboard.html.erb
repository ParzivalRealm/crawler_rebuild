<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h1 class="text-center">Welcome, <%= current_user.email %></h1>
    </div>
  </div>
  <div class="row">
    <div class="col-md-12">
      <% if current_user.admin? %>
        <div class="text-center">
          <%= link_to "Create Scrapper from Attachment", new_scrapper_path(action: "create_with_attachment"), class: "btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>